{
  "name": "@efuller/api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:up:test": "docker-compose -p walking-test -f docker-compose.test.yml up -d",
    "build": "rimraf dist && swc ./src -d dist",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.63",
    "@swc/core": "^1.3.99",
    "rimraf": "^5.0.5"
  }
}
