{
  "name": "@efuller/shared",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc -b tsconfig.build.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.7"
  },
  "dependencies": {
    "dotenv-cli": "^7.4.2",
    "exec-sh": "^0.4.0"
  }
}
